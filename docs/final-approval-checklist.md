# Final Approval Checklist

Everything below this line requires explicit approval from Drizzy.

## 1. Commit locally

Suggested commit message:

```bash
git add .
git commit -m "Build Solana subscriptions allowance controls demo packet"
```

## 2. Create public GitHub repo

Suggested repo name:

```text
superteam-solana-native-subscriptions-allowances-demo
```

Suggested description:

```text
TypeScript demo of AI-agent allowance controls for Solana Native Subscriptions & Allowances.
```

Suggested commands after approval:

```bash
gh repo create dremonkey23/superteam-solana-native-subscriptions-allowances-demo --public --source=. --remote=origin --push
```

## 3. Optional demo video

Record a short terminal walkthrough:

```bash
npm install
npm run check
```

Narration script: `docs/demo-script.md`

## 4. Superteam submission

Listing:

```text
Technical Demo: Solana Native Subscriptions & Allowances Code Sample
```

Submit fields from:

```text
docs/submission-draft.md
```

Required:

- public GitHub repo link
- English README

Optional:

- demo video link
- social proof link

## Hard stop

Do not execute any of these without approval:

- Git commit
- GitHub repo creation
- Git push
- public video upload
- social post
- Superteam account/region action
- final Superteam submit
- no wallet/devnet/mainnet action
